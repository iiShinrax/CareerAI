# CareerAI frontend

The React client calls the CareerAI API directly. It has no demo-response fallbacks: errors from the API are shown to the user.

Run `npm install` and `npm run dev`. Set `VITE_API_URL` when the backend is not at `http://localhost:8000`.
